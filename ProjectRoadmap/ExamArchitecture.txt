Exam System Architecture (Production Ready)
                ┌─────────────────────────┐
                │       TEACHER UI        │
                └────────────┬────────────┘
                             │
                             ▼
                 create_exam_template()
                             │
                             ▼
              ┌──────────────────────────┐
              │      exam_templates      │
              └────────────┬─────────────┘
                           │
                           ▼
              ┌──────────────────────────┐
              │   exam_template_sections │
              │  (A. Phonetics, B. Vocab)│
              └────────────┬─────────────┘
                           │
                           ▼
              ┌──────────────────────────┐
              │    exam_template_items   │
              │  (type + difficulty)     │
              └────────────┬─────────────┘
                           │
                           │ used by
                           ▼

═══════════════════════════════════════════════════════════
                     STUDENT FLOW
═══════════════════════════════════════════════════════════

Student UI
    │
    ▼
choose_template()
    │
    ▼
generate_exam(template_id, grade_level)
    │
    ▼
┌───────────────────────────┐
│           exams           │
│ user_id                   │
│ template_id               │
│ total_questions           │
└──────────────┬────────────┘
               │
               ▼
┌───────────────────────────┐
│       exam_questions      │
│ snapshot question         │
│ question_order            │
└──────────────┬────────────┘
               │
               ▼
┌───────────────────────────┐
│   exam_question_options   │
│ snapshot options          │
└──────────────┬────────────┘
               │
               ▼

═══════════════════════════════════════════════════════════
                     STUDENT TAKES EXAM
═══════════════════════════════════════════════════════════

get_exam(exam_id)
        │
        ▼
frontend render questions
        │
        ▼
student answers
        │
        ▼
submit_exam(exam_id)

        │
        ▼
┌───────────────────────────┐
│        user_answers       │
│ question_id               │
│ selected_option           │
│ is_correct                │
└──────────────┬────────────┘
               │
               ▼

calculate_result()

               │
               ▼

┌───────────────────────────┐
│          exams            │
│ score                     │
│ submitted_at              │
└───────────────────────────┘

               │
               ▼

get_exam_result(exam_id)
               │
               ▼
        Student sees result
📦 Core Tables (Exam Engine)
Template system
exam_templates
exam_template_sections
exam_template_items

Teacher cấu hình đề.

Question bank
questions
options
question_tags

Kho câu hỏi.

Generated exam snapshot
exams
exam_questions
exam_question_options

Snapshot để đảm bảo:

câu hỏi không thay đổi

option không thay đổi

đáp án không thay đổi

Student answers
user_answers
🔐 Security Layer

RPC cần SECURITY DEFINER.

Teacher RPC:

create_exam_template()
update_exam_template()

Student RPC:

generate_exam()
get_exam()
submit_exam()
get_exam_result()
⚙️ Exam Engine RPC Map
Teacher
│
├─ create_exam_template()
├─ update_exam_template()

Student
│
├─ generate_exam()
│
├─ get_exam()
│
├─ submit_exam()
│
└─ get_exam_result()
⚡ Performance Design

Các index quan trọng:

questions(category_id, question_type_id, grade_level, difficulty)
questions(is_active, deleted_at)

question_tags(tag_id)

exam_questions(exam_id)
exam_question_options(exam_question_id)

user_answers(exam_id)
🧠 Production Safeguards

Exam Engine cần các guard sau:

1️⃣ Unfinished exam guard
submitted_at IS NULL
2️⃣ Anti spam
10 seconds throttle
3️⃣ Empty exam guard
IF total_questions = 0
DELETE exam
4️⃣ Snapshot system

Không dùng trực tiếp bảng:

questions
options

Luôn snapshot sang:

exam_questions
exam_question_options


Teacher
   │
   │ create_exam_template()
   ▼
exam_templates
   │
   │
Student
   │
   │ start_exam()
   ▼
exams
   │
   │ get_exam()
   ▼
exam_questions
   │
   │ save_answer()
   ▼
user_answers
   │
   │ submit_exam()
   ▼
score
   │
   │ get_exam_result()
   ▼
review
1️⃣ create_multiple_choice_question
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'create_multiple_choice_question';  OK
2️⃣ update_multiple_choice_question
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'update_multiple_choice_question';  OK
3️⃣ create_essay_question
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'create_essay_question';        OK
4️⃣ build_question_snapshot
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'build_question_snapshot';     OK
5️⃣ import_questions_bulk
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'import_questions_bulk';
6️⃣ import_passage_with_questions_bulk
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'import_passage_with_questions_bulk';
7️⃣ validate_import_questions
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'validate_import_questions';
8️⃣ validate_import_passage_with_questions
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'validate_import_passage_with_questions';
9️⃣ generate_questions_from_item
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'generate_questions_from_item';
🔟 save_answer
select pg_get_functiondef(p.oid)
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where p.proname = 'save_answer';

11 create_passage_question OK